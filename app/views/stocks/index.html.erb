<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Available Stocks</title>
  </head>
  <body>

    <hr>
    <br>
    <h1 class="text-center">Welcome to the 2K Stock Exchange</h1>
    <u><h3 class="text-center">Please select from our available stock options</h3></u>
    <br>
    <hr>
    <br>
    <table class="table table-striped table-hover">
        <th>Ticker</th>
        <th>Company Name</th>
        <th>Last Trade Price</th>
        <th>Market Cap</th>
        <th>EBITDA</th>
        <th>EPS</th>
        <th>Price/Earnings</th>
        <th>50-day Moving Avg.</th>
        <th></th>
        <% @stocks.each do |stock| %>
            <tr>
                <td><%= stock.code %></td>
                <td><%= stock.company_name %></td>
                <td>$<%= number_with_precision(stock.last_trade_price, :precision => 2) %></td>
                <td>$<%= number_with_precision(stock.market_cap, :precision => 2) %>B</td>
                <td>$<%= number_with_precision(stock.ebitda, :precision => 2) %>B</td>
                <td>$<%= number_with_precision(stock.eps, :precision => 2) %></td>
                <td>$<%= number_with_precision(stock.pe, :precision => 2) %></td>
                <td>$<%= number_with_precision(stock.ma_50, :precision => 2) %></td>
                <td type="button" class="btn btn-success"><%= link_to "Buy Shares", new_portfolio_path(:stock_id => stock.id)%></td>
            </tr>
        <% end %>
    </table>
</body>
</html>
